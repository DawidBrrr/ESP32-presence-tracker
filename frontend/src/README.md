# Source Layout

This folder contains the application source code.

- app: Next.js App Router routes and layout
- components: shared UI building blocks
- features: domain modules (auth, devices, telemetry, dashboard)
- hooks: shared React hooks
- services: API and transport clients
- types: shared TypeScript types
- config: runtime configuration and flags
